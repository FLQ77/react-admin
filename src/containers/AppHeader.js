import React from 'react'
import { Layout } from 'antd'

const { Header } = Layout

const AppHeader = props => {
    return (
        <Header>
            <div>
                This is header
            </div>
        </Header>
    )
}

export default AppHeader